export const validateDate = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateTimePicker = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateDay = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateText = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateNumber = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateToggle = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateCheckbox = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateSelect = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};

export const validateTime = (
  value: string,
  isRequired: boolean = false
): boolean => {
  //TODO: Implment validation function
  return true;
};
